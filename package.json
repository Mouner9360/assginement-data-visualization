{
  "homepage": "http://{Mouner9360}.github.io/{assginement-data-visualization}",
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}